const customerId = 123;
let name = "Rudra";
var status = true;
city = "Jajpur";

let state;

/* Never use var 
beacause it does not follow block scope i.e {...} &
functional scope

Also do not use variables without let keyword
*/

console.table([customerId, state, name,  status, city]);